# === CORE WEB ===
Flask==3.0.3
Flask-Mail==0.10.0
Flask-Session==0.8.0
Werkzeug==3.0.3
Jinja2==3.1.4
itsdangerous==2.2.0
MarkupSafe==2.1.5
gunicorn==23.0.0

# === CONFIG ===
python-dotenv==1.0.1

# === VECTOR DB ===
chromadb==0.6.3
chroma-hnswlib==0.7.6

# === EMBEDDINGS ===
sentence-transformers==3.0.1
# → pulls torch, transformers, torch, scikit-learn, scipy, huggingface-hub, pillow, safetensors, regex

# === HYBRID SEARCH ===
rank-bm25==0.2.2

# === COLBERT RERANKING (this is the tricky one) ===
RAGatouille==0.0.9.post2
# → pulls torch, transformers, faiss-cpu, datasets, fast_pytorch_kmeans → and fast_pytorch_kmeans needs:
psutil==5.9.8
# (this is the one that broke your run)

# === LLM ===
groq==0.32.0
langchain-groq==0.3.8
langchain-core==0.3.77
# → pulls langsmith, orjson, pydantic, tenacity, etc.

# === WEB SEARCH ===
ddgs==9.6.0
# → pulls click, lxml

# === VOICE ===
deepgram-sdk==3.8.0
# → pulls aiohttp, websockets, verboselogs

# === PDF ===
PyPDF2==3.0.1

# === STOCK TICKER ===
yfinance==0.2.55
# → pulls pandas, multitasking, lxml, beautifulsoup4, html5lib, peewee

# === AUDIO ===
PyAudio==0.2.14
sounddevice==0.4.6
# → pulls cffi

# === REDIS (real-time + caching) ===
redis==5.0.8

# === MISC THAT STILL GET PULLED (unavoidable) ===
# chromadb brings: grpcio, onnxruntime, opentelemetry stack (10+ pkgs), posthog, typer, etc.
# sentence-transformers brings: torch, transformers, safetensors
# Nothing else you can remove without breaking the app.